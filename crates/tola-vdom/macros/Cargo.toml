[package]
name = "tola-vdom-macros"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Procedural macros for tola-vdom capability system"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full", "parsing"] }
